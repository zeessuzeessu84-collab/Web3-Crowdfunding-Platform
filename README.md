# 🚀 Web3 Crowdfunding DApp

A decentralized crowdfunding platform where transparency is guaranteed by smart contracts. No middleman, just pure peer-to-peer funding.

## 🔥 Why this is "Mast"?
- **Goal Based:** Funds are only useful if the goal is met.
- **Transparency:** Anyone can track how much ETH has been raised.
- **Security:** Built on Solidity ^0.8.20 with reentrancy protection.

## 🛠️ Commands
- `npm install` : Install dependencies.
- `npx hardhat compile` : Contract compile karein.
- `npx hardhat test` : Check if everything works.
